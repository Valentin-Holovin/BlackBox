import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    borderWidth: 1.5,
    borderColor: "#057772",
    width: 331,
    height: 50,
    borderRadius: 8,
    justifyContent: "center",
    alignItems: "center",
    marginVertical: 15,
  },
  text_button: {
    color: "#057772",
    fontSize: 18,
    fontWeight: "600",
  },
});
